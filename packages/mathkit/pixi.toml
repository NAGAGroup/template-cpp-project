# mathkit: header-only library. PACKAGE-ONLY manifest — the single
# workspace at the repo root owns all environments, tasks, and the
# variant matrix. External consumers:
#   mathkit = { git = "<this repo>", tag = "mathkit-v0.1.0", subdirectory = "packages/mathkit" }
[package]
name = "mathkit"
version = "0.1.0"
publish = true

[package.build]
backend = { name = "pixi-build-cmake", version = "*" }

[package.build.config]
extra-args = ["--preset=default"]
# Header-only: no TU includes these headers during the package's own
# build, so without this glob header edits would NOT invalidate pixi's
# build cache.
extra-input-globs = ["include/**/*.hpp"]

[package.run-exports.weak]
mathkit = "0.1.*"
