# enginelib-asan: preset-variant subpackage (package-only manifest; version
# kept in sync with the root package — see docs/versioning.md).
# External consumers:
#   enginelib-asan = { git = "<this repo>", tag = "enginelib-v0.1.0", subdirectory = "packages/enginelib/variants/asan" }
[package]
name = "enginelib-asan"
version = "0.1.0"

[package.build]
backend = { name = "pixi-build-cmake", version = "*" }

[package.build.source]
path = "../.."

[package.build.config]
extra-args = ["--preset=asan"]

[package.host-dependencies]
mathkit = { path = "../../../mathkit" }
spdlog = "*"
stb = { path = "../../../../external/stb" }

[package.run-dependencies]
mathkit = { path = "../../../mathkit" }
