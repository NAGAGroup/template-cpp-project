# enginelib-tests-coverage: instrumented tests against enginelib-coverage.
# BYO clang (llvm coverage instrumentation), like the lib variant.
[package]
name = "enginelib-tests-coverage"
version = "0.1.0"

[package.build]
backend = { name = "pixi-build-cmake", version = "==0.4.5" }

[package.build.source]
path = "../.."

[package.build.config]
extra-args = ["--preset=coverage"]
compilers = []

[package.build-dependencies]
clangxx_linux-64 = ">=22.1,<22.2"
compiler-rt = ">=22.1,<22.2"

[package.host-dependencies]
enginelib-coverage = { workspace = true }
catch2 = { workspace = true }

